# Env / secrets — this repo is public, nothing sensitive may be committed
.env
*.tfvars
*.json.key

# Java / Maven
target/
*.class

# Terraform (.terraform.lock.hcl IS committed — provider version pinning)
.terraform/
*.tfstate
*.tfstate.*

# OS / editor
.DS_Store
.idea/
.vscode/
